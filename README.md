# makeitstickpress.com

The Make It Stick Press homepage — a single static page, deployed to GitHub
Pages by the workflow in `.github/workflows/deploy.yml` on every push to main.

The Amazon link for React That Sticks is a `#amazon` placeholder until the
listing exists — search for it in `index.html` when the book goes live.
